const { get } = require("lodash");
const { bot } = require("../config");
const { infoUser, sendMessageHelper, updateUser } = require("../helpers")
const { dataConfirmBtnEmp } = require("../keyboards/inline_keyboards")

let adminText = {

}

module.exports = { adminText }